# 部署到你自己的域名 + 空间（含图片后台）

这套方案针对**自有域名 + 虚拟主机/空间**：站点是纯静态 HTML（速度快、利于 SEO），
再配一个 **PHP 图片后台**用于登录更换首图和产品图。**不需要数据库，不需要 Netlify/GitHub。**

---

## 你需要先确认 2 件事

1. **空间支持 PHP**（7.0 以上即可）。绝大多数国内/海外虚拟主机都支持；
   如果你的空间是「纯静态托管」，那就没法运行后台，请告诉我，我换方案。
2. **有 FTP/文件管理**可以上传文件（主机商会提供 FTP 地址、账号、密码）。

---

## 一、把域名告诉我，我用正式域名重新生成

网站里的 canonical、hreflang、sitemap 必须写你的真实域名，否则多语言 SEO 会失效。

把域名发我（例如 `www.yourdomain.com`），我会执行：

```bash
SITE_ORIGIN="https://www.yourdomain.com" python build.py
```

生成的 `dist/` 就是可直接上传的完整站点。

---

## 二、上传到空间（FTP）

用 FileZilla（或主机商的文件管理器）把 **`dist/` 目录里的所有内容**
上传到网站根目录，常见根目录名：

| 主机类型 | 根目录 |
|---------|--------|
| 宝塔 / 阿里云 / 腾讯云 | `wwwroot` 或 `public_html` |
| cPanel | `public_html` |
| 西部数码 / 新网等 | `wwwroot` 或 `httpdocs` |

上传后目录结构应类似：

```
根目录/
  index.html          ← 访问域名会跳到 /en/
  admin/              ← ★ PHP 图片后台
  cms/                ← Decap 后台（你用不到，可删）
  assets/
    img/
      hero.jpg        ← 首图
      product/        ← 16 张产品图
  en/  es/  pt/  fr/  de/  ru/  tr/  ar/  ja/  ko/
  sitemap.xml  robots.txt
```

---

## 三、设置目录可写（关键）

后台要覆盖图片文件，所以图片目录必须有写权限。用 FTP 客户端右键改权限，或在主机面板操作：

- `assets/img/` → **755**（不行就 777）
- `assets/img/product/` → **755**（不行就 777）

> 若提示「目录不可写」，后台会明确告诉你是哪个目录、该改成多少，照着改即可。

---

## 四、进入后台换图

1. 浏览器打开 **`http://你的域名/admin/`**
2. 登录：
   - 用户名：`admin`
   - 密码：`WKp88K35CJRwsF`（这是部署时自动生成的随机密码；本仓库为公开仓库，该密码已随代码入库，**上线后请立即在「修改密码」中更换**）
3. **第一件事：在页面底部「修改密码」里改成你自己的密码**
4. 换图：
   - **首图** → 找到「首图 (Hero)」卡片 → 选择文件 → 上传替换
   - **产品图** → 4 款产品各有 4 张，找到对应卡片上传替换
5. 上传成功后，**前台刷新页面即可看到新图**（Ctrl+F5 强制刷新避免缓存）

### 图片规格建议

| 位置 | 建议尺寸 | 说明 |
|------|---------|------|
| 首图 | 1600×900（16:9） | 任意尺寸都会被自动裁成 16:9，不会撑破排版 |
| 产品图 | 1000×1000（1:1） | 正方形最佳 |

支持 JPG / PNG / WebP，单张 ≤ 5MB。

---

## 五、常见问题

**Q：上传后前台图片没变？**
A：浏览器缓存。按 Ctrl+F5 强制刷新，或换个浏览器/无痕窗口看。

**Q：提示「目录不可写」？**
A：把 `assets/img` 和 `assets/img/product` 权限改成 777 再试。

**Q：提示文件太大？**
A：压缩到 5MB 以内再上传（首图 200–500KB 就够清晰了）。

**Q：想改的不只是图片（比如公司名、电话、翻译文案）？**
A：这些是生成时写进 HTML 的，改完需要重新生成再上传。把要改的内容发我，我改好重新生成给你；
或者我可以在后台里加一个「文案编辑」模块，让它写到 JSON 里由前台实时读取。

---

## 六、安全建议

- 登录后**立刻修改默认密码**
- 若长期不用，可把 `admin/` 目录改名（例如 `admin-8f3k/`），降低被扫描到的概率
- 定期备份 `assets/img/` 目录
